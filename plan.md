# 研究计划：星地网络多机器人「导航 × 计算卸载」——从数学建模走向真实物理仿真

> 本文件是**活的研究方案**，随实测结果更新。
> 起意文献：DAOMAN（IEEE TVT 2026）、MAPF-GPT（AAAI 2025）。
> 已读材料：`参考文献/notes/DAOMAN/close-reading.md`、`参考文献/notes/MAPF-GPT/close-reading.md`（两份精读笔记）、Zotero 库相关文献、外部核验结果。
>
> **2026-09-18 更新**：原计划的「可行性验证」已完成并通过（见 §10）。Isaac Sim 早已安装，物理与刚体碰撞实测可用 ——
> §9 的硬件风险已降级，§11 的下一步已改写。当前处于 **P0/P1 交界**。
> 远程： `git@github.com:11anticipate/multi_agents_nav_offload.git`

---

## 0. 一句话定位

**保留 DAOMAN 的问题（星地网络下多机器人"到得了目标"且"算得完任务"），替换它的求解栈，并把它从二维质点运动学搬到 NVIDIA Isaac Sim 的真实刚体动力学里，用「大规模模仿学习 + Transformer」替代「MATD3 + PPO + Hungarian」。**

一句话技术主张：

> 卸载决策的质量不由"每一步选本地/BS/卫星"决定，而由**路径层面的覆盖驻留机会**决定。因此不应把卸载做成与导航并列的第二个 MDP（DAOMAN 的做法），而应把"覆盖—卸载效用"编码进**导航的观测表示**里，用一个统一的去中心化策略同时输出移动与卸载动作。

---

## 1. 两篇起点文献各自给了什么

### 1.1 DAOMAN（IEEE TVT, Vol.75 No.3, 2026, pp.4898–4910）

| 维度 | 内容 |
|---|---|
| 问题 | 多机器人各自去不同目标，途中要避障/避碰，同时把计算任务在本地 / 基站(BS) / 卫星之间做卸载 |
| 方法 | 两阶段：Hungarian 目标分配 → 线性注意力 MATD3 出二维加速度 + 共享 PPO 出离散卸载动作 |
| 场景 | 20×20 km²，3 机器人 / 3 目标 / 3 障碍 / 3 BS / 2 卫星 |
| 结果 | 训练回报 DAOMAN ≈ −45 vs MATD3 ≈ −55 vs MADDPG ≈ −70 |

**它做对的事**（要继承）：
- 抓住了真问题：**移动改变覆盖，覆盖改变卸载选项，卸载又受制于路径** —— 这个耦合是真实存在的。
- 用 CTDE（集中训练、分散执行）处理局部观测，思路正确。
- 按动作空间性质分工（连续用 MATD3、离散用 PPO）是合理的工程直觉。

**它没做的事**（这是我们的全部机会，逐条来自精读笔记第 16 节）：

1. **没有物理**。运动学是 `v ← clip(v + a·ΔT)` 的质点模型，无动力学、无惯性约束、无真实碰撞几何。它的"二维加速度"在真实 AGV 上根本不能直接执行。
2. **规模是玩具级**。3 个机器人在 20×20 km² 里跑 —— 比真实仓库 AMR 车队小两个数量级。
3. **卸载模型过度简化**。信道增益 `h² = 10⁻⁶` 是**常数**；无排队、无回传、无遮挡、无卫星仰角、无多机带宽竞争。
4. **Hungarian 只看初始欧氏距离**，先固定目标再训练，策略不能反过来改分配。
5. **PPO 的观测不含障碍物**（笔记 7.4），即卸载决策看不见会改变路径的东西。
6. **奖励量纲未归一化**，`w = 0.5` 把"米"和"秒"直接相加，语义不稳定，且没做权重敏感性。
7. **没有真正的消融**。MADDPG / MATD3 / DAOMAN 是整体比较，注意力、PPO、Hungarian 各自的贡献无从归因。
8. **没有置信区间、没有多种子、没有成功率/碰撞率**，Fig.7 是单组轨迹。
9. **没有真实世界或高保真仿真验证**（论文自承）。

### 1.2 MAPF-GPT（AAAI 2025, arXiv:2409.00134v5）

| 维度 | 内容 |
|---|---|
| 问题 | 去中心化、部分可观测、无通信、同质合作的多智能体路径规划（MAPF） |
| 方法 | POGEMA 造 3.75M 算例 → LaCAM(10s) 出专家解 → 1B 观测-动作对 → 手工 tokenize 成定长 256 token（词表 67）→ decoder-only Transformer 纯交叉熵模仿学习 |
| 关键设计 | 121 token = 11×11 视野的**归一化 cost-to-go 场**；135 token = 至多 13 个邻居的字段；无因果掩码、非自回归；推理时按概率采样 |
| 结果 | 分布内（Random/Mazes）明显超过 DCC/SCRIMP；推理随智能体数**线性**；2M 小模型在 192 智能体时比 SCRIMP 快 13× |

**它做对的事**（要迁移）：
- **核心贡献是表示与数据，不是算法**。网络是现成的 NanoGPT，学习目标就是一个 5 类交叉熵。
- **cost-to-go 场是地基**：消融 noC2G 让 Random 从 97.6% 掉到 25.8%、Warehouse 从 94.1% 掉到 11.5%。策略自己不认路，认路信息全靠这个字段。
- **中心化专家 → 去中心化策略的蒸馏**：标签来自带全局信息的 LaCAM，输入只有局部观测。
- **刻意不用 RL**：不需要手工设计奖励函数 —— 这一点对"传统马尔可夫/优化积累有限"是**决定性的优势**，我们不必再去调 DAOMAN 那套 `w·r_move + (1−w)·r_offload` 的量纲噩梦。

**它的边界**（要知道）：
- 上界是专家。Puzzles 上 LaCAM 的 SoC 仍显著优于 85M（4 智能体时约 2 倍差距）。
- Warehouse 超 128 智能体时输给 SCRIMP 的 value-based tie-breaking —— 纯模仿学习在极端拥堵下缺一个"反复协商"机制。
- 训练分布窄（17×21、16/24/32 智能体），坐标截断在 [−20, 20]。
- 2M/6M/85M 三个规模同时改了参数量、数据量、迭代数，"越大越好"无法干净归因。
- **完全没有真实机器人或物理仿真验证**。

---

## 2. 外部核验：2026 年的现状（已联网核实）

这几条决定了方案的可行性与新颖性边界，**必须先知道**：

| 事项 | 现状 | 对我们的意义 |
|---|---|---|
| **Isaac Sim 6.0** | 2026-06-08 GA，当前 6.0.1。多后端物理（PhysX + Newton）、**Warehouse Creator 扩展支持 headless**、ROS 2 命名空间改进利于多机、**Isaac Sim MCP**（可被编码助手驱动） | 正是我们要的版本；仓库场景有官方扩展，不必从零建模 |
| **MAPF-GPT-DDG**（Andreychuk et al., IROS 2025, arXiv:2506.23793） | 用 **Delta Data Generation** 主动微调：只针对当前 checkpoint 解不好的"困难算例"补专家数据。**2M 微调后达到原 85M 的水平**，可扩到 100 万智能体 | ⭐ **决定性**。我们不必复现 1B 数据集；用公开权重 + 少量定向数据微调即可，8GB 显存可承受 |
| **STEAM**（arXiv:2605.20929） | Training-free 的拥堵感知增强，直接在 MAPF-GPT 的 logit 上做时空修正，无需重训 | 可作为我们方法的即插即用增强，也可作基线对比 |
| **HMAGAT**（Jain et al. 2026, proroklab） | 超图神经网络做 MAPF，dense warehouse 128 智能体 | 图/超图路线，是我们"Transformer 路线"的主要竞争范式，需在 related work 中明确定位 |
| **MAPF-GPT 权重** | HuggingFace 公开，2M/6M/85M 自动下载，85M 仅 1GB 磁盘 | 直接可用 |
| POGEMA / LaCAM3 / PIBT / SCRIMP / DCC | 均开源 | 专家与基线现成 |

**结论：这条路线的工具链已经全部就位，我们不需要造轮子，只需要做「问题迁移 + 表示扩展 + 物理闭环」这三件新事。**

---

## 3. 研究问题与核心假设

### 3.1 形式化（非马尔可夫式表述，故意的）

我们不把问题写成 POMDP（DAOMAN 那样），而是写成 **"带时变代价场的 MAPF + 沿途任务卸载"**：

给定
- 栅格化场景 `G`（由 Isaac Sim 真实仓库/堆场地图栅格化而来），障碍集合 `O`
- `N` 个同质 AGV，各自起终点 `(s_i, g_i)`，任务在起点生成、在到达时完成
- **时变覆盖函数** `Cov(x, t) → { terrestrial, satellite, none }` 与对应的链路速率 `R(x,t)`、边缘算力 `F(x,t)`
- 每个机器人的待处理数据量 `D_i(t)`（队列）

求一个联合策略 `π`，每个时间步为每个机器人输出 `(move ∈ {↑,↓,←,→,wait}, offload ∈ [0,1])`，其中 `offload` 是**部分卸载比例 ρ**（这是相对 DAOMAN 三档离散动作的扩展）。

目标（多目标，做 Pareto 而非硬加权）：
- 导航：到达时间 / SoC / makespan、成功率、碰撞率、死锁率
- 计算：任务完成时间 = 传输 + 排队 + 计算 + 回传
- 系统：单步决策延迟、规模扩展性

### 3.2 核心假设（Hypothesis，可证伪）

> **H1（表示假设）**：把"覆盖—卸载效用"作为**额外的代价场通道**嵌入局部观测，能让一个纯模仿学习的去中心化策略在任务完成时间上显著优于"MAPF 策略 + 独立卸载头"（即 DAOMAN 式的并列结构），且不牺牲导航成功率。
> 对应消融：`noCoverage` 应产生类似 MAPF-GPT 的 `noC2G` 那样的显著退化。

> **H2（物理假设）**：DAOMAN 式的"直接输出加速度"策略，在 Isaac Sim 的刚体动力学 + 真实碰撞下会显著退化（成功率下降 / 碰撞率上升），因为它训练时所见的质点运动学与真实差速 AGV 的动力学不匹配。
> 这是**我们最有力的批判性证据**，也是"真实仿真环境更能说明问题"这一要求的直接落地。

> **H3（规模假设）**：在 16–32 智能体上训练（受显存约束）的策略，可在 64–192 智能体上零样本运行，因为 tokenization 是定长局部观测、推理复杂度 O(N)。
> 但预期在极端拥堵下会像 MAPF-GPT 一样退化 —— 这正好引出 STEAM 式的 test-time 增强作为第二阶段的改进点。

---

## 4. 技术方案：三层架构

```
┌─────────────────────────────────────────────────────────┐
│  L3  学习层（AI 高级技术就集中在这一层）                    │
│                                                          │
│  中心化专家（离线，可看全局 + 未来覆盖）                    │
│    ├─ 覆盖感知 LaCAM：时间索引的冲突消解                    │
│    └─ 卸载分配：MILP / 贪心（小实例）/ 滚动时域             │
│              ↓  回放，抽取每个机器人的局部观测              │
│  Tokenization（本方案的核心工程贡献）                      │
│    ├─ 121 token：归一化 cost-to-go 场（继承 MAPF-GPT）     │
│    ├─ ★ 121 token：覆盖—卸载效用场（新增通道）              │
│    ├─ 若干 token：自身队列 D_i、任务类型、邻域边缘负载       │
│    └─ 135 token：邻居字段（位置/目标/历史/贪心动作）         │
│              ↓                                            │
│  Decoder-only Transformer（MAPF-GPT 权重微调 / MAPF-GPT-DDG）│
│              ↓  联合动作头                                 │
│    move ∈ 5 类   ⊗   offload ρ ∈ {0, 0.25, 0.5, 0.75, 1}  │
│              ↓                                            │
│  推理：每机独立前向，无通信，O(N)                           │
└─────────────────────────────────────────────────────────┘
                        ↕  动作 = 栅格级期望移动 / 卸载比例
┌─────────────────────────────────────────────────────────┐
│  L2  桥接层（这是"栅格世界 → 真实仓库"的关键，最容易被忽略） │
│    · 局部轨迹跟踪器（栅格动作 → 速度指令）                   │
│    · 死锁 / 阻塞检测与恢复                                 │
│    · 覆盖图栅格化与在线更新                                 │
│    · 计算任务队列与链路仿真（延迟、丢包、抖动）              │
└─────────────────────────────────────────────────────────┘
                        ↕  ROS 2 Jazzy 话题（/tf /scan /cmd_vel /task）
┌─────────────────────────────────────────────────────────┐
│  L1  物理层                                               │
│    NVIDIA Isaac Sim 6.0 + PhysX                          │
│    · 室内仓库（货架/传送带/窄通道）+ 室外堆场/园区           │
│    · 差速/全向 AGV 刚体，真实碰撞几何与接触动力学            │
│    · 域随机化：质量、摩擦、传感器噪声、光照                  │
└─────────────────────────────────────────────────────────┘
```

### 4.1 为什么"覆盖—卸载效用场"是合理的表示扩展

MAPF-GPT 的 cost-to-go 场回答的是：**"从这个格子到我的目标还要几步"**。

我们新增的通道回答一个**同构的问题**：**"如果我现在在这个格子发起卸载，最快的完成时间是多少；以及沿着我可能的路径，最近的'高价值卸载窗口'还有多远"**。

具体可实现的定义（待 P2 阶段定稿，需文献支撑）：

```
U(x, t) = min over 可选卸载节点 k ∈ {local, BS, sat} 可见于 x 的
              [ D_i / R(x, k, t) + D_i / F(k, t) + 回传项 ]
C-C2G(x) = 从 x 出发，沿最短路径到"U 值低于阈值的最小步数"（类似 cost-to-go，
           但在通信代价而非几何距离上做距离变换）
```

**关键性质**：它和 cost-to-go 一样是**逐格整数/可量化**的，因此可以直接套用 MAPF-GPT 的 tokenization 范式（数值 token + 越界 token + ∞ token）。这是这个扩展能成立的技术前提 —— 不是所有"好想法"都能塞进 67 词表的定长序列里，而这个可以。

### 4.2 卸载动作的连续化

DAOMAN 只有 `x ∈ {0,1,2}`（本地/BS/卫星）。我们扩展为**部分卸载比例** `ρ ∈ {0, 0.25, 0.5, 0.75, 1}`（或更细），这是 DAOMAN 自己在局限里承认没做的（精读笔记第 16 节第 2 点）。离散化成 5 档可以保持动作头是分类问题，兼容交叉熵训练。

---

## 5. 与 DAOMAN 的逐项对照（这张表是论文的骨架）

| 维度 | DAOMAN | 本方案 | 改进的性质 |
|---|---|---|---|
| 环境 | 二维质点运动学，20×20 km² | Isaac Sim 6.0 刚体动力学，真实仓库 + 堆场 | 物理保真度 |
| 机器人规模 | 3 | 训练 16–32，测试至 192 | 规模 |
| 求解范式 | CTDE 强化学习（MATD3 + PPO） | 中心化专家 → 去中心化 Transformer 模仿学习 | **范式** |
| 奖励设计 | `w·r_move + (1−w)·r_offload`，量纲未归一 | 无需奖励；多目标做 Pareto 前沿 | 消除调参依赖 |
| 目标分配 | Hungarian（初始欧氏距离，一次性固定） | 融入 MAPF 起终点分配；可支持 lifelong 重分配 | 动态性 |
| 卸载动作 | 离散 3 档 | 部分卸载比例 ρ（5 档离散化） | 表达力 |
| 卸载决策依据 | PPO，观测**不含障碍物** | 覆盖效用场，与导航共享表示 | 耦合方式 |
| 通信模型 | `h²=10⁻⁶` 常数，无排队/回传/遮挡 | 链路预算 + 仰角 + 遮挡 + 排队 + 回传 + 抖动丢包 | 真实性 |
| 注意力 | 线性注意力（critic 内） | Transformer 全局 token 交互（encoder 内） | 建模能力 |
| 消融 | 无 | 继承 MAPF-GPT 的 `noC2G/noGA/noAH/noGoal` + 新增 `noCoverage/noQueue` | 可信度 |
| 统计 | 单条平滑曲线 | ≥5 seeds、95% CI、显著性检验 | 可信度 |
| 验证 | 纯数值仿真 | Isaac Sim + ROS 2 闭环 + 域随机化 | 可信度 |

---

## 6. 分阶段路线

每个阶段都有**可验收的交付物**和**明确的完成判据**。完成一个阶段即 commit 一次（见 `agents.md`）。

### P0 · 立项准备与基线复现 *(预计 2–3 周)*

**目标**：拿到一个能说话的批判性基线，而不是只靠读论文下结论。

| 任务 | 说明 |
|---|---|
| P0.1 | 初始化 git 仓库（当前目录**还不是** git repo），配 `.gitignore` |
| P0.2 | 复现 DAOMAN 的二维环境（PyTorch）：位置/速度/加速度、ΔT=0.1、边界、障碍、BS/卫星覆盖 |
| P0.3 | 补齐 DAOMAN 未写明的细节并**记入 `ASSUMPTIONS.md`**：网络层数、PPO clip/GAE/epoch、随机种子、零加速度分支（式(5)–(6) 有除零问题） |
| P0.4 | 跑通 Hungarian + attention-MATD3 + PPO，对齐 Fig.6 的量级（DAOMAN ≈ −45） |
| P0.5 | 建立**评估协议**：≥5 seeds、95% CI、成功率/碰撞率/到达时间/任务完成时间；修复 DAOMAN 缺的消融（`noAttention` / `noPPO` / `noHungarian` / `w ∈ {0.25,0.5,0.75}`） |

**交付物**：`src/baselines/daoman/`、复现报告 `docs/P0-daoman-repro.md`、`ASSUMPTIONS.md`
**完成判据**：能回答"DAOMAN 的每个模块各贡献多少"——这是原论文没回答的问题，也是我们第一步的独立产出。

---

### P1 · 物理仿真底座 *(预计 4–5 周，工程量最大的阶段)*

**目标**：一个可复现的、带真实动力学的星地网络仓库环境。

> **2026-09-18 状态**：P1.1 已完成 —— Isaac Sim 6.0.1 与 IsaacLab 3.0.0 已安装且实测可用，headless 启动 / GPU 物理 / 刚体碰撞全部通过（§10.2）。剩余的未知不是"能不能跑"，而是"多台 + ROS 2 桥接 + 完整仓库场景"会不会触到显存与工程复杂度的天花板。**因此下一步做垂直切片，而不是逐项铺开。**

| 任务 | 说明 | 状态 |
|---|---|---|
| P1.0 | **垂直切片**：一台差速 AGV + 最小场景 + ROS 2 `/cmd_vel` 闭环。目标是最薄的一条线端到端跑通，暴露 Isaac Sim ↔ ROS 2 ↔ 低层控制的真实问题 | **建议最先做** |
| P1.1 | ~~安装 Isaac Sim 6.0 + ROS 2 Jazzy~~ → Isaac Sim 已就位；**ROS 2 Jazzy 仍需安装** | 部分完成 |
| P1.2 | 用 Warehouse Creator 建场景：**室内仓库**（货架、窄通道、传送带）+ **室外堆场/园区**（卫星可见、地面站稀疏） | 待 `A-006` 拍板 |
| P1.3 | 导入差速 AGV 的 URDF；配置刚体碰撞、摩擦、质量；验证 Gains Tuner |
| P1.4 | ROS 2 多机命名空间：`/robot_{i}/tf`、`/scan`、`/cmd_vel`、`/task`；验证 Isaac Sim ↔ ROS 2 桥接 |
| P1.5 | **覆盖与通信模型**：地面 BS（室内，含遮挡衰减）+ LEO 卫星（室外，含仰角、可见窗口、传播时延）；替换 DAOMAN 的常数 `h²` |
| P1.6 | **计算任务模型**：任务生成（SLAM / 检测 / 点云配准）、队列、传输+计算+回传时延、丢包与抖动 |
| P1.7 | 封装成 gym-style 环境 + headless 批量仿真接口 |
| P1.8 | 域随机化：质量、摩擦、传感器噪声、延迟分布 |

**交付物**：`src/envs/isaac/`、`src/ros2/`、场景 USD、`docs/P1-sim-setup.md`（含版本锁定与安装踩坑记录）
**完成判据**：16 台 AGV 在 headless 下稳定运行 ≥1000 步，ROS 2 话题可被外部节点订阅，碰撞检测正确触发。

> ⚠️ **硬件预警**：本机 RTX 4060 Laptop **8GB 显存 / 30GB 内存**，低于 Isaac Sim 官方建议（RTX 3070 起 / 32GB RAM）。**必须先做 P1.1 的可行性验证**，若失败立即切兜底方案（见 §9）。

---

### P2 · 栅格抽象与中心化专家 *(预计 3 周)*

**目标**：把真实 3D 场景映射成可求解的 MAPF 问题，并造出高质量专家。

| 任务 | 说明 |
|---|---|
| P2.1 | 场景栅格化：Isaac Sim 地图 → 占据栅格；标定栅格分辨率与真实 AGV 尺寸/转弯半径的关系 |
| P2.2 | **时变覆盖图**：`Cov(x,t)` 的栅格化表示与时间索引 |
| P2.3 | 中心化专家 v1：**覆盖感知 LaCAM**（在 LaCAM3 上改造，代价函数含覆盖驻留） |
| P2.4 | 卸载分配子问题：小实例用 MILP 求最优作上界参照；大规模用贪心/滚动时域 |
| P2.5 | 定义"覆盖感知 cost-to-go"（C-C2G）并做单元测试 |
| P2.6 | 数据生成：目标 5–20M 观测-动作对（远小于 1B，但配合 DDG 微调足够） |
| P2.7 | 数据过滤：继承 MAPF-GPT 的去重 + 等待动作采样；记录各类动作占比 |

**交付物**：`src/experts/`、`src/data/`、`docs/P2-expert-and-data.md`
**完成判据**：专家在自己的场景上成功率 ≥ LaCAM 基线；数据流水线可复现、有校验点（未解出比例、等待动作占比）。
**参考**：Zotero 中 Nagai & Okumura 2026《From Gridworlds to Warehouses》正是这一步的核心参照，务必精读。

---

### P3 · 表示与模型 *(预计 3–4 周，本方案的核心贡献)*

**目标**：扩展 tokenization，训练出联合策略。

| 任务 | 说明 |
|---|---|
| P3.1 | 实现扩展 tokenizer：新增覆盖效用场通道；词表扩展；**单元测试**（长度恒定、越界/∞ token、填充规则） |
| P3.2 | 路线 A：从 MAPF-GPT 公开权重（2M/6M）微调 —— 需处理词表扩展后的嵌入层 |
| P3.3 | 路线 B：**MAPF-GPT-DDG 范式**：先跑基线策略 → 检测失败/困难算例 → 只对这些算例补专家数据 → 再微调。**这是显存受限下的最优路线，优先做这条** |
| P3.4 | 联合动作头：move(5) × offload(5) |
| P3.5 | **消融（必须做，这是原论文最缺的）**：`noCoverage`（验证 H1）、`noC2G`、`noGA`、`noAH`、`noGoal`、`noQueue`、数据量 × 参数量交叉 |
| P3.6 | 记录全部超参与随机种子 |

**交付物**：`src/models/`、`src/train/`、消融表 `docs/P3-ablation.md`
**完成判据**：`noCoverage` 相对完整版有统计显著的退化（验证 H1）；至少一条路线超过"MAPF-GPT + 贪心卸载"基线。

---

### P4 · 闭环评估与规模验证 *(预计 3–4 周)*

**目标**：在真实物理环境里把所有方法放在同一把尺子下比。

| 任务 | 说明 |
|---|---|
| P4.1 | 桥接层：栅格动作 → 局部跟踪控制器 → `/cmd_vel`；死锁检测与恢复 |
| P4.2 | **同环境公平对比**：<br>① DAOMAN 复现版（P0 产物，接入本环境）<br>② 纯 MAPF-GPT + 贪心卸载<br>③ 纯 MAPF-GPT + 随机卸载<br>④ 本方案（P3）<br>⑤ 中心化专家（上界参照）<br>⑥ STEAM 增强版（test-time plug-in） |
| P4.3 | 验证 H2：量化 DAOMAN 从质点运动学迁到刚体动力学的退化幅度 |
| P4.4 | 规模泛化：训练 16–32，测试 64/128/192；记录决策延迟曲线 |
| P4.5 | 域随机化鲁棒性：质量/摩擦/延迟抖动/丢包下的性能衰减 |
| P4.6 | 多目标：不做硬加权，画导航时间 vs 任务完成时间的 **Pareto 前沿**（修正 DAOMAN 的 w 语义问题） |

**交付物**：主结果表图、`docs/P4-main-results.md`
**完成判据**：所有对比在同一 Isaac Sim 环境、同一 seeds、带 95% CI 下完成。

---

### P5 · 进阶 AI 技术（可选，视时间与结果决定）*(预计 2–4 周)*

按优先级排列，均在 P4 之后增量加入，**每一项都要有单独消融**：

1. **拥堵感知 test-time 增强**（参照 STEAM）：在 logit 层做时空修正，针对 H3 预期的拥堵退化
2. **图/超图路线对照**（参照 HMAGAT）：与 Transformer 路线做同环境对比，明确定位
3. **扩散模型轨迹生成**（Zotero: `Z9786L4T` 扩散+MARL 引导、`IPS73VRX` Train-Small-Deploy-Large）：用于拥堵场景的多模态轨迹候选
4. **语义任务层**（VLM/LLM）：自然语言任务描述 → 任务类型与优先级；参照 PECMAN / DM³-Nav
5. **lifelong 扩展**：任务持续到达，用 RHCR 类专家微调（MAPF-GPT 在 LMAPF 上仍低于 RHCR，这是明确的开放问题）

---

### P6 · 论文 / 报告撰写

目标 venue 建议（按难度递增）：
- 保底：学位论文 / 中文核心期刊
- 主攻：**IROS / ICRA / CoRL**（机器人仿真 + 学习，重视真实验证）
- 冲刺：**RA-L / IEEE T-RO**（需更强的理论或实机验证）、**AAAI / IJCAI**（偏学习侧）

叙事主线（三句话）：
1. DAOMAN 提出了"导航 × 卸载"的联合问题，但在无动力学的玩具仿真中求解，其结论无法迁移。
2. 我们把该问题搬进 Isaac Sim 的真实物理环境，证明 DAOMAN 式策略在刚体动力学下显著退化（H2）。
3. 我们提出"覆盖感知 cost-to-go"表示 + 中心化专家蒸馏的 Transformer 策略，无需手工奖励，在同一环境中 Pareto 占优（H1）。

---

## 7. 评估协议（先定死，避免事后挑选）

| 类别 | 指标 |
|---|---|
| 导航 | 成功率、Sum-of-Costs、Makespan、碰撞率、死锁率、平均到达时间 |
| 计算 | 任务完成时间（传输+排队+计算+回传）、卸载成功率、非法卸载率、本地/BS/卫星选择分布 |
| 联合 | 导航时间 vs 任务完成时间的 **Pareto 前沿**（不做硬加权） |
| 系统 | 单步决策延迟 vs 智能体数、显存占用、数据生成耗时 |
| 统计 | **≥5 seeds**、95% CI、组间显著性检验；禁止只报单条平滑曲线 |

**所有基线必须在同一 Isaac Sim 环境、同一组 seeds 下重跑。** 不接受"引用原论文数字"。

---

## 8. 创新点清单（可发表性自检）

| # | 贡献 | 类型 | 强度 |
|---|---|---|---|
| C1 | 首个在真实动力学仿真（Isaac Sim + ROS 2）中研究星地网络多机器人"导航×卸载"联合问题的工作，并给出 DAOMAN 式方法的失效分析 | 问题 + 实证 | 中—强（取决于 H2 效应量） |
| C2 | "覆盖感知 cost-to-go"（C-C2G）多通道 tokenization —— 把卸载机会编码进导航表示 | 表示（工程） | 中（类比 MAPF-GPT 的 cost-to-go，需消融支撑） |
| C3 | 中心化联合专家（覆盖感知 LaCAM + 卸载分配）→ 去中心化 Transformer 的蒸馏；部分卸载比例 ρ 的扩展 | 方法 | 中—强 |
| C4 | 完整的 Isaac Sim ↔ ROS 2 ↔ 学习策略闭环、规模泛化与域随机化验证 | 系统 | 中（工程量大，是 C1–C3 的支撑） |

**诚实提醒**：C2 是"MAPF-GPT 的 cost-to-go 洞见在新维度上的类比应用"，不是全新理论。论文里应当这样定位自己，审稿人也会这样看。真正的差异化在 **C1 + C4**（真实物理）和 **C3 的联合专家设计**。

---

## 9. 风险与兜底

| 风险 | 概率 | 兜底方案 |
|---|---|---|
| ~~**Isaac Sim 在 8GB 显存笔记本上跑不动**~~ **已于 2026-09-18 实测排除了报错层面** | ~~中—高~~ → 低 | headless 启动、GPU 物理、刚体碰撞全部通过（见 §10.2）。**剩余风险只剩规模**：多机（>8 台）同屏 + RTX 渲染时的显存压力尚未实测。兜底：① headless + 关 Path Tracing；② 机器人规模降到 4–8；③ 租云 GPU；④ Isaac Sim 只做最终验证，训练侧用轻量 env |
| **磁盘不足**（65GB 可用，Isaac Sim 已占 27GB + IsaacLab 11GB） | 中 | 数据集是最大变量（MAPF-GPT 全量 258GB，绝不可下）。走 DDG 路线只生成增量数据，控制在 10GB 内；定期清理 `~/.cache/ov` 着色器缓存 |
| 专家数据生成太慢 | 中 | 用 LaCAM3 + PIBT（快）；缩小到 5M 对；优先走 DDG（只补困难算例） |
| 微调效果不如从零训 | 中 | 两条路线并行（P3.2 / P3.3），取优者；词表扩展后嵌入层需重新初始化，这是已知难点 |
| H1 不成立（`noCoverage` 无显著退化） | 中 | 这本身是**可发表的负结果**；转向"卸载决策不需要路径级信息"的论证，并把重点移到 C1/C4 |
| H2 不成立（DAOMAN 迁移后没退化） | 中 | 说明质点抽象足够，则把卖点改为"我们给出了 DAOMAN 结论的**有效性边界**"，仍是贡献 |
| MAPF-GPT 词表扩展后无法加载预训练权重 | 中 | 方案 B：覆盖场走**独立的 adapter 分支**，主干权重原样冻结 |
| 范围失控（P5 贪多） | 高 | P5 严格在 P4 之后、且每项单独评估；默认只做第 1 项 |

---

## 10. 本机环境（2026-09-18 实测）

### 10.1 环境清单

```
OS           Ubuntu 24.04.5 LTS        ← 与 ROS 2 Jazzy / Isaac Sim 6.0 匹配 ✓
CPU          32 线程                    ✓
RAM          30 GB（官方建议 32GB）      ⚠ 临界但可接受
GPU          NVIDIA RTX 4060 Laptop / 8188 MiB / sm_89
驱动/CUDA    595.84 / CUDA 12.9（Warp 1.13.0）
磁盘         65 GB 可用（Isaac Sim 已占 27GB + IsaacLab 11GB）  ⚠ 数据集是最大变量
Docker       已装                        ✓
Isaac Sim    6.0.1-rc.7  /home/gsh/isaacsim  (27 GB)  ✓ 已安装
IsaacLab     3.0.0       /home/gsh/IsaacLab   (11 GB)  ✓ 已安装
             venv: IsaacLab/env_isaaclab  Python 3.12.13 / torch 2.11.0 / numpy 2.4.4
ROS 2        未安装（Isaac Sim 的 ros2 bridge 扩展已就位）  ← P1 需装 Jazzy
Git          已初始化，远程 origin = multi_agents_nav_offload  ✓
SSH key      ~/.ssh/id_ed25519.pub 已存在  ✓
```

**首次调研时曾误判 Isa本项目装不动，实际早已安装完整 workstation 版**（当时只扫了工作目录、未看家目录）。

### 10.2 物理保真度实测（这是 P1 能开工的依据）

headless 启动成功（首次含 warmup 约 85 s，之后约 6 s）；Warp 在 `cuda:0 / RTX 4060 Laptop 8 GiB` 上初始化，GPU 物理可用。
零重力自由空间、dt = 1/60 s、0.2 m 立方体对撞：

| 速度 m/s | 每步位移 m | 最小中心间距 m | 判定 |
|---|---|---|---|
| 1 / 2 / 4 / 8 / 16 | 0.017 – 0.267 | ≈ 0.200（= 接触距离） | 正确碰撞 |
| 25 | 0.417 | 0.037 | 穿透（tunneling） |

**结论一**：离散碰撞检测在每步位移远大于半宽时才会穿透，**AGV 真实速度区间（0.5–3 m/s）远在安全区内，无需开启 CCD**。
**结论二**：这张表可直接用作论文的「仿真保真度验证」小节 —— 这正是 DAOMAN 完全没有的东西。

> ⚠️ 测试条件（零重力、无摩擦、默认材质、特定几何）由我们自行设定，写进论文时必须完整交代。见 `ASSUMPTIONS.md` A-002。

### 10.3 已知 API 坑（Isaac Sim 6.0）

| 坑 | 说明 |
|---|---|
| `omni.isaac.core` 不存在 | 已改名为 `isaacsim.core.api` |
| **`isaacsim.core.api` 已在 `extsDeprecated/` 下** | 6.0 主推 `isaacsim.core.experimental.*`。长期项目建议一开始就评估新 core，见 A-001 |
| `PhysicsMaterial` 无 `.apply()` 方法 | 网上旧教程写法失效 |
| USD prim path 不能含 `.` | `/World/a_1.0` 非法，须用整数索引 |

---

## 11. 立即可做的下一步（已按实测结果改写）

~~原第 1 步的可行性验证已于 2026-09-18 完成并通过~~ —— Isaac Sim 能跑、GPU 物理可用、刚体碰撞在目标速度区间内正确（§10.2）。**最大的未知已经排除。**

当前推荐顺序：

1. **决定 Isaac Sim core API 选型`（ ``A-001` `）**——新 core（`isaacsim.core.experimental.*`）还是废弃中的旧 core。这是写代码前唯一还需要定的技术岔路，越晚改代价越大。
2. **做 P1 的垂直切片**（不是"先把碰撞检测做好"）：一台差速 AGV + 最小场景 + ROS 2 `/cmd_vel` 闭环跑通。目的是端到端验证最薄的一条线，暴露真正未知的 Isaac Sim ↔ ROS 2 ↔ 低层控制环节。单独抠碰撞容易滚成没有研究产出的环境工程。
3. **并行推进 P0.2–P0.4**（DAOMAN 复现）：纯 PyTorch，不依赖 Isaac Sim，产出是必需的批判性基线与 H2 的证据。
4. 精读 Nagai & Okumura 2026《From Gridworlds to Warehouses》—— P2 栅格桥接的核心参照。

---

## 12. 需要你 / 老师拍板的决策点

以下问题会影响路线，**在动手前确认比做完再改代价小得多**：

1. **场景是"室内仓库 + 室外堆场"混合，还是纯室外（矿区/港口/灾后）？**（`A-006`）
   - 混合：更贴合老师说的"3D 仓库/工厂"，但室内卫星不可见，星地特色被削弱。
   - 纯室外：星地特色鲜明（LEO 可见窗口是天然的时变覆盖），但离"仓库"远。
   - *我的建议*：混合场景，室外堆场占约 1/3，室内仓库占 2/3 —— 这样"移动改变覆盖"的耦合最强（机器人穿过室内外边界时卸载选项真实切换）。
2. **Isaac Sim core API 用新的还是旧的？**（`A-001`，写代码前必须定）
   - 新 core `isaacsim.core.experimental.*`：官方方向、不会踩废弃坑，但文档与示例少、上手慢。
   - 旧 core `isaacsim.core.api`：文档多、示例全（我这次探针用的就是它），但目录已在 `extsDeprecated/` 下。
   - *我的建议*：**先用旧 core 跑通 P1 垂直切片**（降低首周风险），但在 P1 结束前做一次迁移评估，别等到 P2/P3 大量代码依赖后再改。
3. **ROS 2 装 Jazzy 还是 Humble？** Ubuntu 24.04 官方支持 Jazzy，且 Isaac Sim 6.0 已支持 Jazzy —— 倾向 Jazzy，但需确认实验室是否有版本约束。
4. **机器人数量的目标值？** 这直接决定 8GB 显存够不够。建议 P1 先定 8 台（安全），P4 再压测到 32/64 看崩在哪。
5. **是否要求实机验证？** 若有真 AGV 可用，方案的价值会大幅提升（sim-to-real 是硬通货）；若没有，P4 的域随机化就是能力上限。
6. **P5 的进阶技术要不要做？做几个？** 建议至少做第 1 项（STEAM 式增强），其余视进度。
7. **目标 venue 与 deadline？** 决定 P5/P6 的投入分配。
8. **计算资源**：现阶段本地可用（已验证）。若 P4 要做到 192 机器人，再考虑申请服务器或云 GPU。

---

## 13. 附：关键文献清单

**已在本地 / Zotero：**
- Dong et al., *DAOMAN*, IEEE TVT 75(3), 2026 — 起点与批判对象
- Andreychuk et al., *MAPF-GPT*, AAAI 2025 (arXiv:2409.00134) — 方法论来源
- Nagai & Okumura, *From Gridworlds to Warehouses*, arXiv 2026-05 (`W8R864BD`) — ⭐ 栅格→真实 AGV 的桥接，P2 核心
- Müller, *MARL for Deadlock Handling among AMRs*, 2025 (`B7RWTVQP`) — 死锁处理
- Allman et al., *MARL Warehouse Robots*, 2025 (`5FVQPA4X`) — QMIX/IPPO 仓库基线
- Roy et al., *Modeling and Validation of QoC for Edge-Offloaded Collaborative Navigation*, 2026 (`PBWBUM97`) — ⭐ 卸载+导航耦合的质量控制建模
- Jain et al., *Graph Attention-Guided Search for Dense MAPF*, 2025 (`AV6LKEEX`)
- Arita & Okumura, *Lifelong LaCAM with Local Guidance*, 2026 (`RZEMQW7Z`)
- Fejziaj et al., *On dynamic MAPF methods: review*, 2026 (`T7QGZL2W`)
- Lee et al., *Generative Multi-Robot Motion Planning via Diffusion + MARL*, 2026 (`Z9786L4T`)
- Singh et al., *Train-Small Deploy-Large: Diffusion-Based Multi-Robot Planning*, 2026 (`IPS73VRX`)
- Gao et al., *Differentiable Environment-Trajectory Co-Optimization*, 2026 (`LPBVREUM`)
- Fang et al., *PECMAN*, 2026 (`KSSABSN8`) / Kashiri et al., *DM³-Nav*, 2026 (`4E9RP5I5`) — 语义导航

**外部核验，建议补进 Zotero：**
- Andreychuk et al., *MAPF-GPT-DDG: Advancing Learnable MAPF Solvers with Active Fine-Tuning*, IROS 2025 (arXiv:2506.23793) — ⭐⭐ P3 主路线
- *STEAM: A Training-Free Congestion-Aware Enhancement Framework for Decentralized MAPF*, arXiv:2605.20929 — P5 第 1 项
- Jain et al., *Pairwise is Not Enough: Hypergraph Neural Networks for MAPF*, arXiv:2602.06733 (github: proroklab/hmagat) — 竞争范式
- Okumura, *LaCAM3* (github: Kei18/lacam3)；Skrynnik et al., *POGEMA* — 工具链
