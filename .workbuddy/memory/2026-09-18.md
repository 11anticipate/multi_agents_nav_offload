# 2026-09-18

## 项目立项：星地网络多机器人「导航 × 计算卸载」

用户要求我作为科研导师，从 DAOMAN（IEEE TVT 2026）出发设计方案，参考 MAPF-GPT（AAAI 2025），
引入更多高级 AI 技术，并用 NVIDIA Isaac Sim 做真实物理仿真（避免 DAOMAN 式的纯数学建模）。

### 已读材料
- `参考文献/notes/DAOMAN/close-reading.md`（428 行精读笔记，非常详尽）
- `参考文献/notes/MAPF-GPT/close-reading.md`（586 行精读笔记）
- Zotero 库：MAPF/多机器人导航方向文献约 20+ 篇，已列出关键 key

### 产出（本次只创建这两个文件，未改动任何既有文件）
- `plan.md` — 研究总方案（13 节）
- `agents.md` — 智能体约束文件（三条铁律）

### 核心方案主张
不把卸载做成与导航并列的第二个 MDP（DAOMAN 做法），而是把「覆盖—卸载效用」编码进导航的观测表示，
用一个去中心化 Transformer（MAPF-GPT 权重微调）联合输出 move + offload ρ。
物理层用 Isaac Sim 6.0 + ROS 2 Jazzy，栅格-物理之间加桥接层。

核心假设（可证伪）：
- H1 表示假设：覆盖效用场作为新增 token 通道有显著作用（消融 noCoverage 验证）
- H2 物理假设：DAOMAN 式「直接输出加速度」在刚体动力学下显著退化 → 这是最有力的批判性证据
- H3 规模假设：训练 16-32 可零样本泛化到 192

### 外部核验的关键事实（2026-09）
- Isaac Sim **6.0 于 2026-06-08 GA**，当前 6.0.1；多后端物理（PhysX + Newton）；Warehouse Creator 支持 headless；有 Isaac Sim MCP
- **MAPF-GPT-DDG**（Andreychuk et al., IROS 2025, arXiv:2506.23793）：Delta Data Generation 主动微调，
  2M 微调后达原 85M 水平 → 这是 8GB 显存下唯一可行路线，**不必复现 1B 数据集**
- **STEAM**（arXiv:2605.20929）：training-free 拥堵感知增强，作用在 MAPF-GPT logit 上
- **HMAGAT**（Jain et al. 2026, proroklab）：超图神经网络 MAPF，是 Transformer 路线的竞争范式
- MAPF-GPT 权重 HuggingFace 公开，2M/6M/85M 自动下载

### 本机环境（已实测，影响方案可行性）
- Ubuntu 24.04.5 / 32 线程 / 30GB RAM / **RTX 4060 Laptop 8GB 显存** / 65GB 可用磁盘 / Docker 已装 / **ROS 2 未装**
- ⚠️ 8GB 显存低于 Isaac Sim 官方建议（RTX 3070 起），**这是方案的单点故障，P1.1 必须先做可行性验证**

### 用户偏好（重要）
- 传统马尔可夫决策/数学优化积累有限 → 优先推荐不需要手工奖励与最优性推导的路线（模仿学习/监督学习/序列建模）
- 厌恶「形而上的数学建模」，要求真实仿真环境
- 要求：修改必须有文献或用户同意；每完成小任务即提醒 commit；及时更新文档

### 待用户/老师拍板
1. 场景：室内仓库 + 室外堆场混合（我推荐）vs 纯室外
2. 是否有实机验证
3. P5 进阶技术做几项
4. 目标 venue / deadline
5. 能否申请服务器或云 GPU

---

## 追加：环境实测（推翻了之前的悲观假设）

**重大更正**：之前只扫了 Documents 目录，漏看了家目录。用户机器上已经装着完整的 Isaac Sim。

已实测（非推测）：
- `/home/gsh/isaacsim` 27GB，**Isaac Sim 6.0.1-rc.7+release.42383.32955d8d.gl6.0.1**（workstation 完整安装）
- `/home/gsh/IsaacLab` 11GB，**IsaacLab 3.0.0**，venv `env_isaaclab`（Python 3.12.13 / torch 2.11.0 / numpy 2.4.4）
- 驱动 595.84，CUDA 12.9（Warp 1.13.0），GPU 识别为 `cuda:0 RTX 4060 Laptop 8GiB sm_89`
- ROS 2 桥接扩展**已安装**（isaacsim.ros2.bridge / nodes / tf_viewer / urdf），但系统 ROS 2 未安装
- 可用资产工具：URDF/MJCF importer、conveyor 生成器、omap 生成器、collision_detector、gain_tuner

**所以 plan.md §9「Isaac Sim 装不上」的风险基本解除** —— 8GB 显存跑 headless 物理没问题。
唯一剩余硬约束是磁盘：65GB 可用（Isaac Sim 已占 27GB + IsaacLab 11GB）。

### 碰撞保真度实测结论（脚本 /tmp/isaac_collision_scan.py，零重力自由空间，dt=1/60s）
| 速度 m/s | 每步位移 m | 最小中心间距 m | 判定 |
|---|---|---|---|
| 1 / 2 / 4 / 8 / 16 | 0.017–0.267 | ≈0.200（= 接触距离） | 正确碰撞 |
| 25 | 0.417 | 0.037 | 穿透(tunneling) |

→ **AGV 真实速度 0.5–3 m/s 远在安全区内，无需开 CCD**。只有 ≥25 m/s 才穿透。
→ 这可以直接写进论文的「仿真保真度验证」小节，比 DAOMAN 完全没有物理验证强很多。

### 踩到的 API 坑（务必记住）
- Isaac Sim 6.0 中 `omni.isaac.core` 已不存在 → 用 `isaacsim.core.api`
- **`isaacsim.core.api` 本身已落在 `extsDeprecated/` 下** → 6.0 主推新 core（ `isaacsim.core.experimental.*` ）。
  **长期项目应评估迁移到新 API，否则后期可能踩废弃坑。**
- `PhysicsMaterial` 没有 `.apply()` 方法（旧教程写法失效）
- USD prim path 不能含 `.` → `/World/a_1.0` 非法，要用整数索引
- headless 首次启动 ~85s（含 warmup），之后 ~6s

### 本轮未改动任何项目文件
只在 /tmp 写了探针脚本。用户尚未批准 git init。

---

## 追加：仓库已建立并完成首次推送（2026-09-18 下午）

- **远程仓库**：`git@github.com:11anticipate/multi_agents_nav_offload.git`，branch `main`
- 本地目录保留原名 `多智能体强化学习`（git 不要求与远程同名，避免动用户个人目录结构）—— 记为 A-008
- **首次 commit `7579620`，已 push 到 origin/main**，9 files / +2207

### 本轮新建/更新的文档
| 文件 | 动作 |
|---|---|
| `README.md` | 新建（项目简介、三层路线、环境实测、保真度扫描表） |
| `ASSUMPTIONS.md` | 新建，A-001..A-008 |
| `PROGRESS.md` | 新建（进度看板、待办、待拍板决策） |
| `plan.md` | §2 外链、§9 风险降级、§10 改为实测三小节、§11 改写下一步、§12 增至 8 个决策点、P1 加 P1.0 垂直切片 |
| `agents.md` | 新增文档更新授权说明与 §4.3、放宽 §6-10、新增 §9.1 API 坑 / §9.2 已装资产、§8 清单更新 |
| `.gitignore` | 新建 |

### 关键假设条目（后续务必回查）
- A-001 Isaac Sim core API 选型（旧 core 在 extsDeprecated 下）→ **写代码前必须先决策**
- A-002 碰撞扫描的实验条件是我们自定的，写入论文必须完整交代
- A-006 场景混合方案待用户拍板

### 用户偏好更新（重要）
- **文档更新已获长期授权，无需逐次请示**（原话"文档一定要更新不用问我"）。
  已写入 agents.md §4.3。但技术路线变更/删除代码/引入依赖仍需询问。
- 用户倾向快速推进，不喜欢被反复追问。

### 知识产权处理（主动决策）
默认 `.gitignore` 排除了 `参考文献/*.pdf`：DAOMAN 是 IEEE TVT 正式出版论文，
不应进公开仓库。已告知用户，私有仓库下可用 `git add -f` 强制纳入。

---

## 追加：S1 重构为「新 Core 合同测试」（2026-09-18 下午）

用户主动提出把 S1 从"逐行改写旧探针"重构为 **contract probe**（分层 + 契约快照），
并给出 T0–T7 验收表。**我评估后基本采纳**——它比我原方案好，理由是：
原方案写的是"确认两条 API 路径等价"，但真正的目标应是"新 Core 能否**独立**支撑 P1.0"，二者差别很大。

### 我实测验证了用户的 API 面假设 —— 全部成立
| 用户假设 | 实测结果 |
|---|---|
| `SimulationManager.step()` 存在 | ✅ 签名 `step(*, steps=1, callback=None, update_fabric=False)` |
| `SimulationEvent` 存在 | ✅ 14 个成员，在 `impl/simulation_event.py` |
| `core.experimental.objects` 有 GroundPlane / Cube | ✅ 另有 Sphere/Capsule/Cone/Cylinder/Plane/Mesh/Camera/Light |
| `core.experimental.prims` 有 GeomPrim / RigidPrim / XformPrim | ✅ 另有 Articulation/Prim/DeformablePrim/BufferDtype |
| `RigidPrim` 有 get_world_poses / get_velocities / apply_collision_apis | ✅ 均有 |

### 我新挖到的 3 个坑（用户和文档都没提）
1. **`SimulationManager.step()` 的 `steps` 是 keyword-only** → `step(100)` 报错，必须 `step(steps=100)`
2. **`RigidPrim.get_world_poses()` / `get_velocities()` 返回 Warp 数组**（`tuple[wp.array, wp.array]`），不是 numpy，需 `.numpy()`
3. **每个扩展自带 `config/python_api.md`** —— 比网上文档更可靠的本地权威签名来源，查 API 先查它

### 我对用户 S1 的增补（已写入 plan.md）
- **T1 新增**：断言 `get_simulation_time()` 在 step 后严格递增 → 抓"step 了但什么都没发生"的静默假通过
- **T5 新增**：确定性复跑两遍，否则 S1.2 的"基线"不成立
- **S1.0 提前查 physics scene 的 dt / gravity 默认值来源**，别等 S1.1 数值对不上才发现
- **产物归属分层**（这是我与用户意见不同的地方，已记为 A-009）：
  用户说"S1 全部只放 /tmp，不进仓库"，但又说 S1.2 扫描数据"值得留下"——**这两条冲突**。
  我建议：脚本 S1.0–S1.3 放 /tmp，但 **S1.4 基线报告必须进仓库** `docs/P1.0-core-probe.md`，
  稳定后脚本提升到 `tools/probes/`。理由：只留 /tmp 会导致下次会话必须重跑且无法被论文引用。

### 本次文档改动（未提交）
- `plan.md`：新增「P1.0 细则：S1 新 Core 合同测试」整节（分层表、T0–T9 验收表、范围边界、产物归属）；
  §11 下一步改写；§12 决策点 2 标记为已决议并附依据；P1 表 P1.0 行改写；硬件预警段落改写为已解除
- `ASSUMPTIONS.md`：A-001 从 OPEN → RESOLVED（附本机实测依据）；新增 A-009 / A-010 / A-011
- `agents.md`：§9.1 重写（改为新 core 为准 + 新增 6 项实测 API 事实 + 已确认类清单）；§10 变更记录
- `PROGRESS.md`：进行中 / 待办 / 决策表全部更新

### 未做的事
**没有执行 S1**（没跑探针）。用户本轮只要求规划，且末尾问"还是不要先改"。
我只更新了文档（用户已授予"文档更新无需请示"），执行需用户明确放行。

---

## 追加：S1 全部执行完毕（2026-09-18 下午）

用户批准全部 5 条建议后开工。结果：**S1.0–S1.4 全部跑完，新 Core 可独立支撑 P1.0**。

### 结果
- S1.0 API 面：PASS，13 个类全部存在，签名已核对
- S1.1 Falling Cube：**ALL PASS**。z 1.0→0.2479（静止高度=半高 0.25），vz→0，无穿透，无 NaN
- S1.1 T5 确定性：**max|Δ| = 0.000e+00（bit 级一致）**
- S1.2 速度扫描：1/2/4 → 0.200（与旧 Core 一致）；8/16 → 0.133（**旧为 0.217/0.206，有偏差**）；
  25 → 0.083（旧 0.037，均穿透，被撞体位移 0 = 完全穿过）
- S1.2 CCD：`enable_ccd(True)` **完全无效果**（两表逐点相同）
- S1.3 Restart / Existing Stage：**ALL PASS**
- S1.4 报告：`docs/P1.0-core-probe.md`（新建 docs/ 目录）

### 最重要的技术发现（务必记住，后续所有仿真代码都依赖）
**推进方式必须改为 (b)：先 `app_utils.play()`**。
原计划的 (a) `SimulationManager.step()` 单独调用**不驱动物理** —— 时间递增、步数递增，但刚体一动不动。
这是本项目目前为止最阴的坑：不报错、指标看起来正常，只有刚体不动。
`play()` 之后两者都可用（`simulation_app.update()` 主用，`step(steps=N)` 需手动步进时用）。

**"物理静默不跑"三件套**（缺任一都会静默失效）：
1. `app_utils.play()`
2. `SimulationManager.set_default_physics_scene(path)`
3. `PhysicsScene.set_gravity(...)`（默认未 author，读出来 NaN）

### 其他实测坑（已写入 agents.md §9.1）
- step() 的 steps 是 keyword-only
- RigidPrim 返回 Warp 数组需 .numpy()
- apply_collision_apis 在 GeomPrim 不在 RigidPrim
- set_local_poses 用 translations=；set_world_poses 才用 positions=
- SimulationManager 无 play/pause/reset/stop
- stage 无 clear_stage；重建 stage 会让旧 prim 失效 → 用原地 set_world_poses + set_velocities 复位
- Cube/GroundPlane 自带 positions/sizes/scales，无需 XformPrim
- 确定性复跑必须"两遍都从同一复位态开始"（我第一版测法错了：第一遍从中途开始采样，导致误判）

### 本轮文件改动（未提交）
- 新建 `docs/P1.0-core-probe.md`
- `agents.md` §9.1 补入"物理静默不跑三件套"与其他实测坑
- `ASSUMPTIONS.md` A-002 补入新 Core 复跑数据与偏差说明
- `PROGRESS.md` S1 全部标记完成，待办更新

### 探针脚本位置（本轮留在 /tmp，未提升进仓库）
`/tmp/s1_0_api.py`、`/tmp/s1_1_falling_cube.py`、`/tmp/s1_1_diag.py`、`/tmp/s1_2_sweep.py`、`/tmp/s1_3_lifecycle.py`"还是不要先改"。
我只更新了文档（用户已授予"文档更新无需请示"），执行需用户明确放行。
